## Next steps (verified-scope only)

1. Add/extend `run_pipeline()` contract tests for output envelope and required indicator keys.
2. Implement only missing indicator behavior in existing feature/filter/scoring modules and `run_advanced_modules` wiring.
3. Add focused scenario tests (replay/live) to verify decision, confidence, and blocker reasons after minimal code changes.
